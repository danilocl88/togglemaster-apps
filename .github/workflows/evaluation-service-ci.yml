name: CI - evaluation-service

on:
  workflow_dispatch:

  pull_request:
    branches:
      - main
    paths:
      - 'evaluation-service/**'
      - '.github/workflows/evaluation-service-ci.yml'

  push:
    branches:
      - main
    paths:
      - 'evaluation-service/**'
      - '.github/workflows/evaluation-service-ci.yml'

permissions:
  contents: read

concurrency:
  group: evaluation-service-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:

  quality-security:
    name: Build / Test / Security
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: 'evaluation-service/go.mod'
          cache-dependency-path: 'evaluation-service/go.sum'

      - name: Download dependencies
        working-directory: evaluation-service
        run: go mod download

      - name: Go test
        working-directory: evaluation-service
        run: go test ./...

      - name: Go vet
        working-directory: evaluation-service
        run: go vet ./...

      - name: SAST - gosec
        run: |
          docker run --rm             -v "$PWD:/src"             -w "/src/evaluation-service"             ghcr.io/securego/gosec:2.28.0             -severity high             -confidence high             ./...

      - name: SCA - govulncheck
        id: govulncheck
        uses: golang/govulncheck-action@v1.1.0
        with:
          work-dir: evaluation-service
          repo-checkout: false
          go-version-file: evaluation-service/go.mod
          go-package: ./...
          output-format: json
          output-file: govulncheck.json

      - name: Trivy filesystem - CRITICAL gate
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: 'evaluation-service'
          format: table
          severity: CRITICAL
          exit-code: '1'

      - name: Docker build
        run: |
          docker build             --tag togglemaster/evaluation-service:${GITHUB_SHA}             evaluation-service

      - name: Trivy image - CRITICAL gate
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'togglemaster/evaluation-service:${{ github.sha }}'
          format: table
          severity: CRITICAL
          exit-code: '1'
